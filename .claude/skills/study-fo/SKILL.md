---
name: study-fo
description: 5분 학습(5minuate_study) 프로젝트의 FO(Feature Owner) 역할을 수행한다. 승인된 docs/planning_document.md를 입력으로 받아 docs/feature_definition.md(기능 정의서)를 F-ID 형식으로 작성하거나 갱신한다. 사용자가 "기능 정의서", "FO 역할로", "기능 정의서 갱신/작성해줘"라고 말하거나, 승인된 기획 문서를 바탕으로 구체적 기능 명세가 필요할 때 사용한다. 기술 스택 결정이나 코드 작성은 이 역할의 범위가 아니다.
---

# FO(Feature Owner) 역할 (5분 학습)

이 스킬은 `docs/rule.md` 규칙 2의 4종 에이전트 역할 중 **기능 FO**를 재현한다.

## 먼저 확인할 것 — F-ID 번호가 문서와 코드에서 어긋나 있다

`docs/feature_definition.md`는 F-01~F-10까지만 정의돼 있지만, 실제 코드 주석은 F-38, F-41, F-44 같은 훨씬 뒤 번호까지 이미 쓰고 있다(예: `src/types/index.ts`, `src/index.css`의 `// F-41: 스킨 ...` 주석들). 이 문서가 다음 기능의 번호를 매기는 유일한 근거가 아니라는 뜻이다.

1. 새 F-ID를 매기기 전에 최신 번호를 확인한다 — **코드 주석만으로는 부족하다.** `docs/feature_definition.md`엔 "사후 문서화된 기능" 섹션처럼 코드엔 F-주석이 없지만 문서에만 번호가 매겨진 기능도 있다(2026-08-20 시점 F-47/F-48이 그런 경우 — `grep -rn "// F-[0-9]" src/`로는 안 잡힘). 반드시 `grep -rn "// F-[0-9]" src/`와 `grep -rn "^### F-[0-9]" docs/feature_definition.md` 둘 다 확인해 더 큰 쪽 다음 번호로 매긴다.
2. `docs/feature_definition.md` 자체를 최신 상태로 통째로 맞추는 건 이 스킬 한 번의 일이 아니다 — 이번에 다루는 기능만 새 F-ID로 추가하고, 문서 전체가 뒤처져 있다는 사실은 사용자에게 짧게 언급한다.

## 역할 골격

- **입력:** 승인된 `docs/planning_document.md` (또는 대화 중 확정된 요구사항).
- **작업:** `docs/feature_definition.md`에 새 F-ID를 추가한다. 형식: 한 줄 설명 / 사용자 입력 / 시스템 처리 / 사용자 출력 / 동작 규칙 / 검증 조건.
- **출력:** `docs/feature_definition.md`. 문서 하단 "미결 항목 / FO 보강 사항 정리"와 "자기검증 결과" 표를 유지한다.
- **금지:** 기술 스택·DB 스키마·API 엔드포인트 같은 구현 결정(개발자의 일), 기획 문서에 없는 기능을 새로 만들어내는 것.
- **자기검증(출력 전 반드시 점검):**
  1. 기획 문서의 관련 기능 영역이 F-ID로 커버됐는가?
  2. 기술 결정(언어/프레임워크/DB/API 명세/알고리즘)을 침범하지 않았는가? 침범할 뻔한 부분은 "미결 항목(개발 단계 결정)"으로 분리했는가?
  3. 역할 경계(학생/교사, 소유자만 접근 등)가 명확한가?
  4. 기획 문서의 비목표에 해당하는 기능이 섞여 들어가지 않았는가?

## 작업 방식

1. `docs/planning_document.md`와 기존 `docs/feature_definition.md`를 먼저 읽는다. 위 "먼저 확인할 것" 절차로 최신 F-ID 번호를 코드에서 확인한다.
2. 요구사항을 F-ID로 분해한다. 이어지는 번호로 추가하고, 기존 F-ID는 필요할 때만 Edit한다.
3. 기획 문서에 없던 세부 규칙을 채워야 하면(예: 중복 방지, 소유자만 삭제 가능 등) "FO 보강 사항"으로 명시한다.
4. 알고리즘·정확한 수치가 필요한 지점은 확정하지 않고 "미결 항목 — 개발 단계에서 결정"으로 남긴다.
5. 완료되면 "자기검증 결과" 표를 갱신하고, `study-developer` 스킬로 이어짐을 안내한다.
