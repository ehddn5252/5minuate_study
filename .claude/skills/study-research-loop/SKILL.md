---
name: study-research-loop
description: 5분 학습(5minuate_study) 프로젝트를 위해 인터넷에서 좋은 기획 아이디어를 찾아 Planner가 초안을 쓰고 FO와 논의해 다듬은 뒤, 바로 구현·배포까지 이어가는 자율 연구-기획-구현 루프. 사용자가 "기획자가 인터넷 검색해서 짜고 FO랑 논의 후 반영", "리서치 루프 돌려", "아이디어 찾아서 계속 추가해줘"라고 말할 때 사용한다. 문서만 갱신하는 study-improve-loop(기존 문서-코드 괴리 점검)와 달리, 이 스킬은 외부 리서치가 출발점이고 실제 코드/콘텐츠 구현까지 매 사이클 완료한다.
---

# 연구-기획-구현 루프 (5분 학습)

`study-planner` → `study-fo` → `study-developer` 세 역할을 한 사이클 안에서 순서대로 실행하되, Planner의 입력을 "사용자 요구사항"이 아니라 **웹 검색으로 찾은 아이디어**로 바꾼 자율 루프다. 사용자가 "문서 → 바로 구현까지, 멈추라고 할 때까지 계속"이라고 명시적으로 요청했으므로, 이 루프는 `study-planner`/`study-fo`가 원래 요구하던 승인 대기 없이 한 사이클 안에서 끝까지 진행한다.

## 매 사이클 절차

1. **리서치 (Planner 역할 시작)**: `docs/ceo_prompt.md`의 비전("공부를 해야하지만 게으른 사람을 위한 공부 앱", 목표 이탈 방지)과 현재 제품 형태(목표·퀴즈·커리큘럼·스킨·마스코트·학원 모드, `git log --oneline -20`으로 최근 방향 확인)를 먼저 파악한다. 그 다음 WebSearch로 다음 중 한 방향을 좁혀서 검색한다 — 매 사이클 같은 검색어를 반복하지 않는다:
   - 학습과학/교육심리 기법(간격 반복, 인출 연습, 능동 회상, 습관 형성, 동기부여 넛지 등)이 이 앱의 기존 메커니즘(라이트너 상자, 오답노트, 배지, 연속 학습일)에 어떻게 더 적용될 수 있는지
   - Duolingo·말해보카 등 이미 이 코드베이스가 오마주하고 있는 앱들의 구체적 UX 패턴(커밋 로그에 "듀오링고/말해보카 오마주"로 언급된 것들) 중 아직 없는 것
   - PWA/모바일 웹 학습 앱의 접근성·성능·오프라인 지원 관련 모범 사례
2. **기획 초안 (Planner)**: 찾은 아이디어 중 이 프로젝트의 비전·성공기준과 정렬되는 것 하나를 골라 `docs/planning_document.md`의 관련 섹션에 짧게 반영한다. `study-planner` 스킬의 "먼저 확인할 것"(문서가 v1.0에 멈춰 있고 "5분 내 완결"은 폐기된 원칙) 경고를 그대로 따른다.
3. **기능 정의 (FO)**: Planner의 초안을 받아 `study-fo` 스킬대로 `docs/feature_definition.md`에 새 F-ID를 추가한다. `study-fo` 스킬의 F-ID 번호 확인 절차(코드 주석 + 문서 양쪽 확인)를 그대로 따른다 — 코드 주석 grep만 믿으면 번호가 겹칠 수 있다(사이클 1에서 실제로 겹칠 뻔함, F-47은 이미 "목표 삭제" 기능에 쓰여 있었음). **단, 이번 사이클이 사용자 기능이 아니라 개발 도구/프로세스(예: 빌드 설정, 린트 설정) 수정이면 F-ID를 억지로 붙이지 않는다** — feature_definition.md의 F-ID 템플릿(사용자 입력/시스템 처리/사용자 출력)은 사용자 기능 전용이다. 이런 경우 이 스킬 파일의 "지금까지 이 루프가 반영한 것" 기록에만 남기고 넘어간다(사이클 7 사례 참고).
4. **논의 (Planner ↔ FO)**: FO가 작성한 F-ID가 Planner의 원래 의도·비전과 어긋나지 않는지, 혹은 기술적으로 이 스택(PWA+Supabase+Gemini+Cloudflare Workers, 무료 티어)에서 실현 가능한지 짧게 재검토한다. 어긋나면 F-ID를 조정한다. 이 저장소엔 사람이 실시간으로 없으므로 이 "논의"는 스스로 두 관점을 번갈아 점검하는 자기검증이다.
5. **구현 (Developer)**: `study-developer` 스킬대로 진행한다. 필요하면 `docs/architecture_decision.md`를 갱신하고, 코드 또는 콘텐츠(예: `docs/english_content_spec.md` 기준 시드 데이터)를 작성한다.
6. **검증**: `npx tsc --noEmit`, `npm run build` 통과 필수. UI 변경이면 가능한 범위에서 실제로 렌더링해 확인한다.
7. **배포/커밋**: 코드가 바뀌었으면 `CLAUDE.md` 지침대로 `npm run build && npx wrangler deploy`까지 자동 실행한 뒤 커밋한다. 콘텐츠(DB 시드)만 추가했다면 배포는 생략하고 커밋 메시지에 "Supabase SQL 에디터에서 사용자가 직접 실행 필요"를 명시한다.
8. **보고**: 이번 사이클에서 무엇을 검색했고, 왜 그 아이디어를 골랐고, 문서에 어떻게 반영했고, 실제로 뭘 구현했는지 짧게 보고한다. 매 사이클 전체를 장황하게 설명할 필요는 없지만 "무엇을 왜 선택했는지"는 항상 남긴다.

## 스코프 가드레일 (필수)

- `docs/ceo_prompt.md`의 비전(게으른 사람이 목표를 포기하지 않게)과 무관한 아이디어는 채택하지 않는다. 비목표 항목은 현재 비어있으므로(2026-08-20 기준) 대신 **기존 제품의 실제 형태**(목표 관리·퀴즈·커리큘럼·스킨·학원 모드)를 암묵적 스코프로 삼는다 — 완전히 새로운 제품 축(예: 소셜 네트워크, 커머스)을 만들지 않는다.
- 한 사이클에 아이디어 하나만 채택한다. 검색 결과가 여러 개 유망해도 다음 사이클로 미룬다.
- 예산 제약(무료 티어)을 벗어나는 아이디어(유료 API, 유료 인프라)는 채택하지 않는다.
- 이미 있는 기능의 재발명이 아닌지 `src/`를 먼저 확인한다.
- **그 기능이 실제로 켜져 있는지도 확인한다.** 코드가 존재한다고 해서 그 코드가 실행되는 건 아니다
  (사이클 5에서 `services/notification.ts` 전체가 CEO 요청으로 비활성화돼 App.tsx가 아예 호출하지
  않는 상태였는데 이를 구현 다 끝내고 배포 직전에야 발견함). 구현 착수 전에 관련 모듈이 실제
  진입점(App.tsx, 라우터, 다른 화면)에서 import/호출되는지 `grep -rn "from.*'해당모듈경로'" src/`로
  먼저 확인한다.

## 루프 지속/중단

`/loop`(dynamic mode, 약 5분 간격)로 실행한다. 사용자가 "멈춰"라고 할 때까지 계속하기로 합의했으므로(2026-08-20), 스스로 판단해 멈추지 않는다 — 다만 같은 사이클이 연속으로 실패하거나(빌드 실패 반복 등) 더 이상 스코프 안에서 채택할 아이디어를 못 찾으면 그 사실을 사용자에게 명확히 알리고, 다음 지시를 기다린다.

## 지금까지 이 루프가 반영한 것 (기록)

- **사이클 1 (2026-08-20):** "retrieval practice / spaced repetition app design" 검색 → 정답을 보기 전 먼저 답을 산출하는 게 재인식(객관식)보다 기억에 낫다는 결과를 얻었으나, 이미 F-45(정답 전 음성 녹음)로 커버돼 있어 채택하지 않음. 대신 프로젝트 자체 커밋 로그/문서를 훑다가 "스트릭 프리즈"가 여러 기획 초안 문서(`feature_definition_engagement.md` 등)에서 이미 설계됐지만 실제 코드에는 전혀 없다는 걸 발견해 이걸 채택. F-49로 문서화하고 `types/index.ts`, `GoalCreateScreen.tsx`, `TestScreen.tsx`, `SessionCompleteScreen.tsx`에 구현 → 커밋.
- **사이클 2 (2026-08-20):** "Duolingo gamification mechanics" 검색 → 리더보드/하트(생명력) 시스템이 나왔지만 리더보드는 F-37 주석에 이미 비목표로 명시돼 있고, 하트 시스템은 "포기를 구조적으로 막는다"는 이 앱의 원칙과 정면으로 충돌해(오답 시 진행을 막는 벌칙 장치) 기각. 검색 방향을 PWA 접근성으로 전환 → `prefers-reduced-motion`(동작 줄이기 접근성 설정) 미대응을 발견 — `animate-*` 클래스가 10개 파일에서 쓰이는데 어디도 이 설정을 확인하지 않고 있었음. 순수 CSS로 F-50 구현(`src/index.css`) — 로딩 스피너/녹음중 표시는 기능적 모션이라 예외 처리. Playwright의 `reducedMotion: 'reduce'` 컨텍스트로 실제 동작 검증 후 배포.
- **사이클 3 (2026-08-20):** 학습과학 방향으로 "Leitner box 완전 리셋 vs 한 단계만 후퇴" 검색 → 오답 시 완전 리셋이 오히려 정석(학계 컨센서스)이고 이미 그렇게 구현돼 있어 변경 불필요로 결론(현행 유지가 맞다는 걸 리서치로 확인한 케이스). 인터리빙도 이미 F-09/D-4로 구현 확인. PWA 방향으로 재전환해 "offline detection UX banner" 검색 → `navigator.onLine`/`online`/`offline` 이벤트 기반 배너가 업계 표준 패턴인데 이 앱엔 오프라인 처리가 전혀 없었음(grep 0건). F-51 `OfflineBanner` 컴포넌트 구현, App.tsx 양쪽 라우터 브랜치(로그인 전/후)에 마운트. 처음엔 `fixed` 오버레이로 만들었다가 스크린샷에서 헤더와 겹치는 걸 발견해 일반 흐름 요소로 수정. Playwright `context.setOffline()`으로 실제 online/offline 이벤트 발화 검증 후 배포.
- **사이클 4 (2026-08-20):** PWA 성능(Core Web Vitals/이미지 lazy-load/폰트 로딩) 검색 → 이 앱의 `<img>` 4개는 전부 above-the-fold라 lazy-load 대상이 아니고, 커스텀 웹폰트 자체가 없어 폰트 로딩 이슈도 없고, 이미지는 이미 고정 정사각형 크기라 CLS 문제도 없음을 확인(전부 "이미 문제없음" 결론). 의존성도 5개뿐이라 번들 크기를 안전하게 줄일 여지가 마땅치 않음. PWA capability 쪽으로 재전환해 매니페스트 `shortcuts` 미설정을 발견 — 앱 아이콘 길게 누르기로 "복습"·"오답노트"에 홈 화면도 거치지 않고 바로 진입 가능. F-52로 `vite.config.ts`의 manifest에 shortcuts 2개 추가(새 아이콘 자산 없이 OS 기본 아이콘 사용, 스코프 최소화). 빌드 후 `manifest.webmanifest`에 정확히 반영됨을 확인 후 배포.
- **사이클 5 (2026-08-22, 세션 재개):** "lapsed user re-engagement notification win-back" 검색 → 오래 쉰 사용자에게는 일반 리마인더가 아니라 컴백 환영 톤이 재참여율에 낫다는 결과. `services/notification.ts`를 보니 매일 도는 리마인더가 1일 쉰 사람과 2주 쉰 사람에게 완전히 같은 문구를 보내고 있어 F-53으로 7일 이상 이탈 시 다른 문구 뱅크를 쓰도록 구현·검증(로직 시뮬레이션으로 임계값 분기 확인) 완료. **그런데 배포 단계에서 `wrangler deploy`가 "No updated asset files to upload"를 반환해 조사해보니, `services/notification.ts` 전체가 App.tsx에서 아예 import되지 않는 상태였다** — 학습 알림이 2026-07-22부터 CEO 요청으로 비활성화돼 있었고(App.tsx 주석에 이미 명시돼 있었음), 파일만 재활성화 대비용으로 남아있었던 것. 코드 자체는 안전하고 정확하지만(빌드에서 완전히 tree-shaking돼 번들에 안 들어감, 즉 해가 없음) 지금 당장 사용자에게 아무 효과가 없다. 되돌리지 않고 "재활성화 시 바로 동작하도록 미리 구현해둔 것"으로 문서화하되(F-53, planning/feature 문서에 ⚠️ 명시), **가드레일에 "구현 착수 전 관련 모듈이 실제로 import/호출되는지 먼저 grep으로 확인" 항목을 추가**해 재발 방지.
- **사이클 6 (2026-08-22):** "icon only button accessible name aria-label WCAG" 검색 → WCAG 4.1.2(Level A, 가장 기본 요건)이고 실제로 웹 전체에서 가장 흔한 실패 유형 중 하나(WebAIM 2025 조사 27.7%)라는 근거 확인. 코드베이스에서 뒤로가기 화살표 아이콘(`M15 19l-7-7 7-7`)이 22개 화면에서 반복되는데 `aria-label`이 단 하나도 없었음. Node 스크립트로 21개 화면을 일괄 처리(정규식으로 버튼 태그에 `aria-label="뒤로"` 삽입) 후 dry-run으로 먼저 검증, `CalendarScreen.tsx`는 같은 아이콘을 "뒤로"와 "이전 달"에 둘 다 써서 자동화 대상에서 빼고 손으로 "뒤로"/"이전 달"/"다음 달" 세 가지로 정확히 구분. 스크립트가 만든 들여쓰기 오차(2칸)도 후속 스크립트로 보정. F-54로 문서화. 부수 발견: `npm run lint`가 `eslint.config.js` 부재로 아예 실행이 안 되는 상태 — 스코프 밖이라 손대지 않고 판단 보류에 기록.
- **사이클 7 (2026-08-22):** 사이클 6의 판단 보류 항목(ESLint 미작동)을 이번 사이클 아이디어로 채택 — 리서치 대신 직전 사이클이 남긴 기술 부채를 처리. `git log --all` 확인 결과 이 저장소엔 `eslint.config.js`가 한 번도 존재한 적이 없어(devDependencies는 이미 v9 flat-config용으로 다 설치돼 있었는데도) `npm run lint`가 실제로는 단 한 번도 실행된 적이 없는 상태였음. 표준 Vite react-ts flat-config 템플릿으로 `eslint.config.js` 작성 → 처음 실행하니 `jsx-a11y/media-has-caption` 규칙을 찾을 수 없다는 에러 2건 발생, 원인은 `VoiceRecorder.tsx`/`RecordingsScreen.tsx`에 있던 disable 주석이 애초에 설치된 적 없는 플러그인을 가리키는 죽은 주석이었던 것 — 둘 다 제거. 남은 진짜 린트 지적 11건(에러 6·경고 5, 대부분 미사용 변수/훅 의존성)은 "한 사이클에 아이디어 하나만" 원칙을 지키기 위해 이번엔 손대지 않고 다음 사이클 후보로 남김. **이 수정은 사용자 기능이 아니라 개발 도구 수정이라 F-ID를 붙이지 않기로 FO 단계에서 판단**(feature_definition.md의 사용자 입력/처리/출력 템플릿과 맞지 않음) — 이 판단을 이 스킬 3단계 절차에 가드레일로 남김. `tsc --noEmit`·`npm run build` 통과 확인.
- **사이클 8 (2026-08-22):** Duolingo UX 방향으로 "streak calendar widget engagement design" 검색 → iOS 홈 화면 위젯이 스트릭 유지율을 크게 올린다는 결과였으나, PWA는 OS 네이티브 홈 화면 위젯을 만들 수 없어(네이티브 앱 전용 API) 스택 제약상 채택 불가로 기각. 스트릭 프리즈(F-49)·마일스톤 정체성 서사(F-22)·설치 유도 배너(기존 `InstallBanner.tsx`)도 이미 다 구현돼 있어 이 방향에서 새로 채택할 게 없었음. 학습과학 방향으로 "메타인지 자신감 판단(confidence judgment) 후 정답 확인" 검색도 해봤으나, 2026-08-03 리서치 요약("Judging Your Own Learning Doesn't Always Improve It")이 효과가 일관되지 않다고 지적하고 "시작 마찰 제로" 원칙과도 충돌해(문항마다 확인 탭 하나 추가) 기각. 마지막으로 PWA 접근성 방향에서 코드를 직접 훑다가 `BottomNav.tsx`의 활성 탭 표시가 색상(시각 신호)에만 의존하고 스크린 리더에는 전혀 전달되지 않는 걸 발견 — WCAG `aria-current` 관례를 검색해 확인한 뒤(단, `role="tab"`/`aria-selected`는 실제 라우트 이동이라 잘못된 패턴임을 확인하고 배제) F-55로 `aria-current="page"` 추가·검증·배포.
- **사이클 9 (2026-08-22):** 학습과학 방향(SM-2식 적응형 간격 반복)은 현재 고정 라이트너 간격(1/3/7/16/35일)을 대체할 만큼 근거가 확실하지도 않고 "5분 앱" 스코프에 비해 데이터 모델 변경이 과하다고 판단해 착수 전에 기각. 대신 코드를 훑다가 `SettingsScreen.tsx`에 이미 '차콜'/'네이비' 다크 배경 스킨이 있는데도, 저장된 설정이 전혀 없는 진짜 첫 방문자는 기기가 다크 모드여도 항상 밝은 기본 배경으로 시작한다는 걸 발견. "다크 모드는 선택이지 강제가 아니다, 첫 실행엔 시스템 설정을 따르는 게 표준"이라는 리서치 결과에 따라 F-56으로 `getAppState()`가 `localStorage`에 저장된 값이 아예 없을 때만 `prefers-color-scheme: dark`를 확인해 `bgTheme`을 기존 '차콜' 값으로 기본 설정하도록 구현(새 스킨을 만들지 않고 기존 옵션만 재사용, 한 번이라도 저장된 사용자는 절대 덮어쓰지 않음). Playwright로 (다크+빈 스토리지→차콜), (라이트+빈 스토리지→그레이), (다크+저장된 그레이→그레이 유지) 3가지 시나리오를 실제 렌더링까지 확인 후 배포.
- **사이클 10 (2026-08-22):** "말해보카 힌트/오타 관용" 검색은 별 성과가 없었지만, 그 과정에서 이 앱의 단답형 문제가 애초에 문자열 자동 비교가 아니라 사용자 자기채점(self-judge) 방식이라 "정답인데 오타로 틀렸다고 나옴" 문제 자체가 설계로 이미 회피돼 있음을 확인(새로 할 일 없음). "if-then 계획 수립(implementation intentions)" 검색에서는 목표 달성률을 2~3배 올린다는 강한 근거(94개 연구 메타분석)를 찾았으나, 이걸 구현하려면 목표 생성 화면에 "언제/어디서 할지" 입력을 새로 추가해야 해서 이 프로젝트의 "원칙 2 — 시작 마찰 제로"와 정면충돌하고, 게다가 리마인더로 그 계획을 실제로 상기시켜줄 알림 시스템은 사이클 5에서 확인했듯 CEO 요청으로 비활성 상태라 지금 만들어도 실효가 없어 기각(알림 재활성화 시점에 다시 검토할 후보로 기록). 대신 PWA 접근성 방향에서 사이클 8·9가 손댄 `BottomNav.tsx`를 다시 살펴보다가 비활성 탭 색(`text-gray-400`)의 흰 배경 대비가 약 2.54:1로 WCAG 1.4.3 본문 텍스트 기준(4.5:1)에 크게 못 미치는 걸 계산으로 확인 — `text-gray-500`(≈4.83:1)로 F-57 조정, Playwright 스크린샷으로 활성/비활성 구분이 여전히 잘 되는지 확인 후 배포. `text-gray-400`이 다른 화면에도 많이 쓰이지만 전수 교체는 스코프가 훨씬 커서 이번엔 하단 탭 바만.
- **사이클 11 (2026-08-22):** "교사 대시보드 학생 과제 완료 현황" 검색 → "실시간 추적으로 소외되는 학습자를 더 잘 찾아낸다(22% 개선)"는 결과. 그동안 10번의 사이클이 전부 학생 개인 화면만 다뤘다는 걸 인식하고 처음으로 학원(교사) 영역으로 방향을 틀었음. `ClassDetailScreen.tsx`의 학생 명단을 보니 이름만 나열돼 있고, 미제출 숙제를 확인하려면 학생을 한 명씩 펼쳐야만 했음(펼치기 전엔 아무 신호 없음). F-58로 `listClassOverdueCounts(classId, studentIds)`를 추가해 명단 각 줄에 "⚠️ 미제출 N건" 배지를 미리 보여주도록 구현 — 학생 수만큼 반복 조회하지 않도록(N+1 방지) 반 전체 단위로 쿼리 2번만 쓰도록 설계해 무료 티어 제약도 지킴. 실제 Supabase 로그인 세션을 재현할 수 없어 로직 검토·타입체크·컴파일된 CSS로 렌더링 확인까지만 하고, 실제 계정으로의 데이터 정확성 확인은 사람 몫으로 문서에 명시(F-58 검증 조건에 미완료 체크박스로 남김). 계획 문서(`planning_document.md`)에도 그동안 없었던 "학원(교사) 영역" 섹션을 처음 신설.
- **사이클 12 (2026-08-22):** "테스트 중 이탈 시 beforeunload 경고" 아이디어를 검토했으나, `TestScreen.tsx`에 이미 F-19(진행 상황 즉시 저장)가 있어 중단해도 잃을 게 없고, 오히려 경고 다이얼로그는 "포기를 구조적으로 막는다"는 원칙과 반대로 이탈을 겁주는 나쁜 UX 안티패턴이라 스스로 기각. PWA 방향으로 전환해 "iOS Safari beforeinstallprompt 미지원" 검색 → 애플이 이 이벤트를 의도적으로 구현하지 않아 iOS Safari에선 자동 설치 프롬프트 자체가 불가능하고, 공유 시트로 직접 "홈 화면에 추가"를 눌러야 한다는 걸 확인. 기존 `InstallBanner.tsx`가 이 이벤트에만 의존해 아이폰 사용자에겐 배너가 영원히 안 뜨고 있었음을 발견 — F-59로 iOS Safari 전용 감지(`isIOSSafari()`, iPad 데스크톱 모드의 Mac 위장 UA까지 처리하고 CriOS/FxiOS 등 iOS 내 타 브라우저는 제외)와 안내 문구형 배너를 추가. 6가지 대표 User-Agent로 판별 로직을 단위 테스트하고 Playwright로 렌더링까지 확인 후 배포.
- **사이클 13 (2026-08-22):** 학습과학("endowed progress effect")·Duolingo("goal gradient")를 검색했으나 둘 다 코드베이스에 이미 반영돼 있음을 확인(진행률 바 색상 단계, HomeScreen의 "거의 다 왔다" 문구 — 후자는 짧은 목표에서 조기 노출을 막는 가드까지 이미 있었음). 진행률을 인위적으로 부풀리는 "endowed progress"의 문자 그대로의 적용(신규 목표를 0%가 아닌 값으로 시작)은 학생 스스로의 정확한 진행 상황 추적을 왜곡하는 것이라 교육 앱에는 부적절하다고 판단해 채택하지 않음. 대신 F-58이 스스로 남긴 판단 보류 항목("마감 임박까지 확장할지")을 "early-alert 시스템은 이미 늦은 뒤가 아니라 그 전에 신호를 줘야 실효가 있다"는 ed-tech 리서치로 뒷받침해 채택. 단, 같은 리서치가 "위험도가 다른 신호를 뭉뚱그리면 alert fatigue로 무시당한다"고도 경고해서, F-60으로 `listClassOverdueCounts`를 `listClassDueSummary`로 확장하되 "이미 마감 지남"(빨강)과 "오늘 마감"(호박색) 배지를 의도적으로 분리해 구현. 세 가지 배지 조합(둘 다/하나만/없음)을 Playwright로 렌더링 확인 후 배포.
- **사이클 14 (2026-08-22):** StudyMaterialsScreen(커리큘럼 탭)을 살펴봤지만 이미 완결도가 높아 새로 채택할 게 마땅치 않았음. `nextReviewAt`(라이트너 상자가 계산하는 다음 복습일) 사용처를 grep해보니 스케줄링에만 쓰이고 사용자에게는 단 한 번도 보여준 적이 없다는 걸 발견 — "간격 반복 앱은 다음 복습 시점을 보여주는 게 시스템 신뢰를 쌓는 업계 통용 패턴"이라는 리서치로 뒷받침해 F-61 채택. `TestScreen.tsx`의 `applySrsUpdate`에서 정답 처리마다 갱신되는 `nextReviewAt` 중 세션 내 최댓값만 추적(오답은 항상 내일로 리셋돼 신호 가치가 없어 제외 — 이 설계 덕에 이 값은 구조적으로 항상 3일 이상만 나옴)해 `SessionCompleteScreen`에 "가장 오래 기억한 문제는 N일 뒤에 다시 만나요" 한 줄로 노출. 완료 화면의 "성취 카드"(최대 5개 제약이 있는 기존 배열)에는 넣지 않고 기존 XP/성장 피드백과 같은 담백한 텍스트 톤으로 맞춤. Playwright로 (XP+성장피드백+복습안내), (복습안내만) 두 조합의 구분선이 안 겹치는지 렌더링 확인 후 배포.
- **사이클 15 (2026-08-22):** "교사가 학생 답안에 구체적 코멘트를 남겨야 학생이 실제로 행동에 옮긴다"는 ed-tech 리서치 → 이 앱의 숙제 채점은 자동 점수(score/total)뿐이고 교사가 글로 코멘트를 남길 방법이 전혀 없었음(academy.ts 전체에 comment/feedback 관련 코드 0건). F-62로 채택하되, 이번엔 **이 루프 최초로 DB 스키마 변경(컬럼 추가+RPC 함수)이 필요**했음 — 기존 마이그레이션 폴더(`supabase/migrations/001~014`) 관례를 그대로 따라 `015_submission_comment.sql` 작성. 컬럼에 직접 update 권한을 주면 같은 행의 score/answers까지 함께 열리는 보안 허점이 생기므로(011의 `submit_assignment`가 이미 같은 이유로 RPC를 쓰고 있었음), `set_submission_comment` RPC가 함수 내부에서 "이 제출물 담당 교사 = 현재 로그인한 사람"을 직접 확인한 뒤에만 코멘트 컬럼 하나만 갱신하도록 설계. 클라이언트 코드(교사 쪽 인라인 편집 UI, 학생 쪽 표시)는 마이그레이션 실행 여부와 무관하게 안전하게 배포 가능하다고 판단해(RPC 미존재 시 그냥 에러 메시지로 실패, 크래시 없음) 평소대로 빌드+배포하되, **마이그레이션 파일은 사람이 Supabase SQL 에디터에서 직접 실행하기 전까지 기능이 실제로 동작하지 않는다는 걸 커밋 메시지와 F-62 문서에 명시**(F-53 사례처럼 "구현은 끝났지만 당장 효과 없음" 패턴, 이번엔 CEO 비활성화가 아니라 DB 마이그레이션 미실행이 원인). Playwright로 4가지 상태(코멘트 없음/편집 중/저장됨-교사뷰/학생뷰) 렌더링 확인.
- **사이클 16 (2026-08-22):** 지난 사이클(F-62)이 DB 스키마 변경까지 다뤄서 리스크를 조금 낮추고 싶어, 이번엔 순수 클라이언트 코드로만 끝나는 아이디어를 의도적으로 찾음. "번호 키로 객관식 선택" 등 데스크톱 단축키 리서치는 이 앱이 모바일/PWA 우선(터치 타깃 44px 등 전면 적용)이라 우선순위가 낮다고 판단해 보류. 대신 화면들을 훑다가 `MyQuestionBookScreen`("내 문제집" = 북마크)이 문제·정답·해설을 곧바로 다 보여주기만 해서 사실상 "다시 읽기"이지 "다시 풀기"가 아니라는 걸 발견 — 이 앱이 인출 연습(retrieval practice) 원칙을 다른 곳(F-45, 자기채점 단답형 등)에선 이미 잘 지키고 있는데 정작 북마크 화면만 예외였음. `RetryWrongScreen`(오답 다시 풀기)이 이미 정확히 같은 문제(문제 목록을 받아 실제로 풀게 하는 것)를 잘 풀어놓은 검증된 패턴이라 그 구조를 그대로 재사용해 F-63 `BookmarkReviewScreen`(`/bookmark-review`) 신설 — 여러 목표를 가로지르는 북마크를 섞어서 풀게 하고, 오답이면 그 문제가 속한 목표의 오답노트에도 함께 쌓이게 함. 새 파일이라 `npm run lint`까지 함께 돌려 기존 11건 지적에서 늘어나지 않았음을 확인. Playwright로 (진입 버튼/진행 중/완료) 3가지 화면 렌더링 확인 후 배포.
- **사이클 17 (2026-08-22):** PWA 방향으로 "manifest screenshots richer install UI" 검색 → 크롬이 manifest에 스크린샷이 있으면 기본 텍스트 설치 프롬프트 대신 실제 앱 화면을 보여주는 바텀시트를 띄운다는 걸 확인. 이 앱은 F-52로 shortcuts는 이미 추가했지만 screenshots는 없었음. F-64로 채택하되, 로그인 이후 화면은 가짜 데이터를 지어내야 해서 의도적으로 배제하고 로그인 없이 볼 수 있는 실제 미리보기 경로(`/shorts/english_grammar`, 로그인 화면의 "계정 없이 먼저 둘러볼래요" 칩과 같은 경로)를 Playwright로 실제 캡처 — 사실은 처음엔 로그인 화면 자체를 캡처했다가 스크린샷이 너무 휑해서(빈 여백이 많음) 실제 콘텐츠가 보이는 화면으로 바꿈. 모바일(narrow)·데스크톱(wide) 두 폼팩터를 캡처하고, 처음 2배 스케일로 찍었더니 421KB나 나와서 1배로 다시 찍어 134KB로 줄임. 이 이미지들은 앱 렌더링에 안 쓰이고 OS 설치 다이얼로그 전용이라 `workbox.globIgnores`로 오프라인 프리캐시에서 제외 — 빌드 전후 프리캐시 용량이 정확히 동일(53개, 763.88 KiB)함을 확인해 검증. `manifest.webmanifest` 산출물에 스크린샷이 올바른 크기로 들어갔는지, `sw.js`엔 전혀 없는지 직접 확인 후 배포.
- **사이클 18 (2026-08-22):** WCAG 1.4.10(리플로우, 320px 폭에서 가로 스크롤 없어야 함)을 Playwright로 로그인/미리보기 화면에 직접 테스트했으나 문제없음을 확인(rem 기반 Tailwind 유틸 덕분 — 새로 할 일 없음, "이미 문제없음" 확인 케이스). Duolingo식 "스트릭 복구"(끊긴 뒤 되살리기)도 검토했으나 F-49의 스트릭 프리즈(끊기기 전에 미리 보호)와 성격이 겹쳐 별개 안전장치를 또 만드는 건 과설계라 기각. 대신 `MyAssignmentsScreen.tsx`의 "공지" 탭이 항상 전체 건수만 보여줄 뿐 "새로 온 게 몇 건인지"는 전혀 알려주지 않는다는 걸 발견 — "안 읽음 배지는 (a) 나중에 읽음 처리가 가능하고 (b) 알림이 잦지 않을 때 쓰라"는 알림 UX 리서치가 학원 공지(교사가 가끔 올림, 탭 클릭으로 즉시 읽음 처리 가능)에 정확히 들어맞아 F-65 채택. `utils/storage.ts`에 `lastSeenAnnouncementAt`(localStorage)을 추가하되, 기능이 막 생긴 시점에 기존 공지 전부가 갑자기 "안 읽음"으로 뜨지 않도록 저장된 값이 없으면 최신 공지 시각으로 조용히 채워 넣고 0건에서 시작하도록 설계. 기기 간 동기화는 스키마 변경이 필요해(F-62 사례처럼) 이번엔 로컬 저장으로 스코프를 가볍게 유지. Playwright로 (배지 있음/탭 활성화로 배지 없음) 렌더링 확인 후 배포.
- **사이클 19 (2026-08-22):** Web Share API 사용 여부(`shareCard.ts`)를 확인해보니 이미 구현돼 있었고(새로 할 일 없음), 커리큘럼 탭도 이미 완결도가 높았음. `ClassDetailScreen.tsx`를 다시 훑다가 이미 굉장히 정교한 "오답 통계"(문항별 오답자 수) 탭까지 있는 걸 새로 발견했지만, 정작 숙제 카드 자체(펼치기 전)에는 몇 명이 제출했는지 보여주는 게 전혀 없다는 걸 확인 — F-58/F-60이 "학생 축"에서 이미 풀었던 것과 정확히 같은 문제가 "숙제 축"에는 남아있었던 것. F-66으로 `listClassAssignmentCompletionCounts(classId)`를 추가해 숙제 카드에 "✅ 제출 N/M" 배지를 붙임 — 숙제·학생 수와 무관하게 쿼리 3번으로 집계(N+1 방지, F-58/F-60과 동일한 설계 원칙 재사용), 특정 학생 대상 숙제도 대상 판별 로직을 그대로 재사용. F-58/F-60과 대칭을 이루는 기능이라는 걸 문서에 명시. Playwright로 (부분 제출/전원 제출+대상 인원 배지 동시 표시) 렌더링 확인 후 배포.
- **사이클 20 (2026-08-22):** 학원 축(ClassDetailScreen)만 세 사이클 연속(F-58/F-60/F-62/F-66) 파고든 것 같아 방향을 완전히 바꿔 학습과학으로 복귀. `LearningScreen.tsx`의 "오늘의 핵심 내용" 요약이 전부 밋밋한 평문 불릿이라는 걸 발견하고 "Mayer의 신호화(signaling) 원칙" 검색 → 핵심을 시각적으로 표시하면 메타분석 기준 중간 크기 효과(d=0.52), 특히 사전지식이 적은 학습자에게 더 효과적이라는 근거 확인(단, "과하게 쓰면 역효과"라는 경고도 함께). F-67로 채택 — `services/gemini.ts`의 두 콘텐츠 생성 프롬프트에 "항목당 핵심어 1개만 `**굵게**` 표시" 지시를 추가하고, `LearningScreen.tsx`에 `**...**`만 딱 처리하는 최소 마크다운 파서를 구현. 구현 중 실제 버그를 하나 발견해 고쳤음 — 기존 불릿 제거 정규식이 `**핵심어**`가 항목 맨 앞에 올 때 앞쪽 `*` 하나만 불릿으로 오인해 잘못 떼어냈고(음성 재생 텍스트가 깨질 뻔함), `*(?!\*)`(단독 `*`만 불릿으로 인정)로 고쳐 세 곳(재생/재생속도 변경/화면 렌더링) 모두 통일. Node로 5가지 케이스 시뮬레이션 + `npm run lint`(기존 11건 유지) + Playwright 렌더링까지 확인 후 배포. 실제 Gemini API가 지시를 얼마나 잘 지킬지는 이 환경에서 재현 불가라 판단 보류로 남김.
- **사이클 21 (2026-08-22):** `GoalCompleteScreen.tsx`(목표 완주 화면)를 다시 살펴봤지만 이미 "새 목표 만들기" CTA·공유·뱃지까지 완비돼 있었고, 스와이프 제스처 UX도 검토했으나 정답 선택처럼 되돌리기 어려운 동작에 쓰기엔 오조작 위험이 커서 기각. 대신 `role="alert"`/`aria-live` 사용 여부를 grep해보니 코드베이스 전체에 **단 한 곳도 없다는** 걸 발견 — 폼 검증·API 실패 시 뜨는 빨간 에러 메시지(최소 11개 변수, 8개 파일)가 지금까지 스크린 리더 사용자에게 전혀 알려지지 않고 있었음(WCAG 4.1.3 상태 메시지). F-68로 채택하되, "마감 지남" 라벨이나 정답/오답 색상처럼 항상 존재하는 정적 빨간 텍스트는 `role="alert"`를 붙이면 오히려 오용(불필요한 반복 안내)이 되므로 제외하고, 진짜 동적 에러 상태(`useState`로 관리되는 `error`류 변수)만 정확히 골라 12곳에 적용. Playwright의 `getByRole('alert')`로 실제 접근성 트리 노출까지 확인(로그인 필요한 화면들이라 실제 앱에서 에러를 재현하진 못하고, 속성 정확성 검증까지만 완료) 후 `npm run lint`(기존 11건 유지) 확인하고 배포.
- **사이클 22 (2026-08-22, 사용자가 "이제 멈추고" 상태 확인 요청 → 세션 sql 마이그레이션 실행 확인 후 "계속 진행하자"로 재개):** F-68(role="alert")과 같은 흐름으로 `htmlFor`/`id` 사용 여부를 grep해보니 코드베이스 전체(라벨 쓰는 화면 6개, `<label>` 14곳)에 단 한 건도 없다는 걸 발견 — 라벨이 시각적으로만 입력창 옆에 있었지 실제로 연결된 적이 없었음. 14곳을 하나씩 확인해 입력을 라벨로 감싸는 이미 올바른 패턴(체크박스 등, 3곳)과 형제 요소로만 나란히 있어 연결이 안 된 패턴(9곳)을 구분하고, 단일 입력에 대응하지 않는 그룹 라벨("난이도")·읽기전용 표시("주제")는 대상이 없어 제외. F-69로 9곳에 `htmlFor`/`id` 쌍 추가(목표 생성/수정, 숙제 생성, 수업자료 첨부, 반 만들기, 학원 참여). Playwright의 `page.getByLabel(...)`로 라벨 텍스트만으로 입력창을 찾을 수 있는지 정적 HTML로 검증(정상 인식) + `npm run lint`(기존 11건 유지) 확인 후 배포. 이 사이클 중간에 사용자가 "멈추고 상태 확인" 요청 → cron 잡을 취소하고 진행 상황을 보고 → 사용자가 "sql 다 실행했다, 계속 진행하자"로 응답해 cron을 재생성하고 이 사이클(조사해뒀던 F-69)을 이어서 완료.
- **사이클 23 (2026-08-22):** F-69가 스스로 남긴 판단 보류("`<label>` 자체가 아예 없고 placeholder만 있는 입력창은 다음 사이클에서 확인")를 그대로 채택. 코드베이스 전체 `<input>`/`<textarea>`를 다시 훑어(체크박스·파일 제외) `<label>`이 아예 없는 15곳(닉네임, AI 문제 생성 주제·참고자료, 문제 직접 추가 폼 전체 4곳, 학원 공지·숙제 코멘트·수업자료 제목/내용, 반 참여 코드, 개인 메모, 퀴즈 단답형 답안 2곳)을 7개 파일에서 찾음. F-69와 달리 이번엔 새 `<label>` 요소 대신 `aria-label`을 썼다 — 특히 "문제 직접 추가" 폼처럼 좁은 공간에 필드가 촘촘히 붙어 있어 시각적 라벨을 새로 넣으면 레이아웃이 바뀌는 곳이 많았기 때문. F-70으로 15곳 모두 처리 후, 수정한 두 주요 파일에서 `<input>`/`<textarea>` 총 개수와 처리된 개수(htmlFor/aria-label/래핑라벨)를 하나씩 대조해 빠짐없이 처리됐는지 재확인. Playwright의 `getByLabel()`로 대표 사례 4개 정상 인식 확인 + `npm run lint`(기존 11건 유지) 후 배포 — 이걸로 코드베이스의 모든 텍스트/날짜/멀티라인 입력창이 접근 가능한 이름을 갖게 됨.
- **사이클 24 (2026-08-22, 변경 없음 — 리서치로 현재 설계가 이미 맞다는 걸 확인한 케이스):** 학습과학 방향으로 "elaborative interrogation(정교화 질문, '왜?' 질문)" 검색 → Dunlosky의 2013년 권위 있는 학습전략 리뷰에서 "moderate utility"로 평가되고 효과크기가 0.85~2.57에 달하는 강한 근거를 확인. `QuizCard.tsx`를 보니 이미 `D-5: 자기설명 효과`라는 주석과 함께 "왜 틀렸는지 설명하기"(음성 녹음 + AI가 답변 내용의 정확성까지 평가하는 피드백)가 구현돼 있었음 — 단, 오답일 때만 노출됨. 리서치 자체는 정답에도 적용 가능하다고 하지만, 모든 정답마다 이 무거운 UI(음성 녹음+AI 피드백)를 노출시키면 "칭찬은 빠르게"(원칙 4)라는 이 앱의 명시적 설계 원칙과 정면충돌하고, 매 세션 대다수를 차지하는 정답 순간마다 화면이 무거워져 사이클 8·10에서 비슷한 이유로 기각했던 사례들과 같은 함정에 빠질 위험이 있다고 판단. `getSpeakingFeedback` 프롬프트도 이미 발음이 아니라 "답변 내용이 적절한지, 틀린 부분이 무엇인지"를 우선 평가하도록 설계돼 있어 자기설명 효과의 핵심(내용 정교화)을 이미 제대로 구현하고 있음을 확인. 결론: 현재 스코프(오답에만 적용)가 리서치와 제품 원칙 사이의 의도적이고 올바른 절충이라고 판단해 변경하지 않음 — 사이클 3(라이트너 상자 완전 리셋 확인)과 같은 유형의 "이미 맞게 돼 있음을 확인한" 사이클.
- **사이클 25 (2026-08-22):** `<select>` 요소 자체가 코드베이스에 없어 드롭다운 라벨 후속 작업은 스코프 자체가 없음을 확인. 대신 F-66이 스스로 남긴 판단 보류 항목("반 전체의 평균 제출률을 TeacherHomeScreen에 보여줄지")을 채택 — 다만 "평균 제출률"이라는 단일 퍼센트 숫자보다, F-60의 "이미 마감 지남 vs 오늘 마감" 분리 교훈을 이어받아 "선생님이 운영하는 모든 반을 통틀어 마감 지난 숙제가 몇 개고 몇 명이 밀렸는지"를 보여주는 쪽으로 구체화. F-71로 `getTeacherOverdueSummary()` 추가 — 기존 `getTeacherTodaySummary`(오늘 마감만 봄)와 완전히 별개 함수로 분리하고, 반·숙제·학생 수와 무관하게 쿼리 3번으로 집계(N+1 방지, F-58/F-60/F-66과 동일한 설계 원칙 재사용). 선생님 홈에 빨간 배너를 기존 호박색 "오늘 마감" 배너 위에 쌓이도록 배치해 더 급한 신호를 먼저 보게 함. Playwright로 두 배너가 함께 쌓였을 때의 렌더링 확인 + `npm run lint`(기존 11건 유지) 후 배포.
- **사이클 26 (2026-08-22, 세션 재개 후 첫 사이클):** `<select>`가 없다는 걸 재확인한 뒤 PWA 방향에서 "Badging API"(`navigator.setAppBadge`/`clearAppBadge`)를 검색 — 데스크톱 Chrome/Edge와 iOS 16.4+ 설치형 PWA는 지원하지만 **Android Chrome은 지원하지 않는다**는 걸 명확히 확인. `grep -rn "setAppBadge" src/` 결과 0건이라 완전히 새로운 PWA capability. F-71(사이클 25)이 이미 계산해둔 `overdue.overdueAssignmentCount`를 그대로 재사용해 새 쿼리 없이 배지만 얹는 순수 부가 기능으로 설계 — F-72로 `src/utils/badging.ts`에 `setAppBadgeCount`/`clearAppBadge`를 추가하고 `'X' in navigator` 지원 감지 + `.catch(() => {})`로 미지원/실패 시 조용히 무시하도록 구현. `TeacherHomeScreen.tsx`의 `refresh()` 끝과 `handleSignOut()` 시작에 각각 연결. 이 기능은 OS 아이콘을 건드리는 특성상 Playwright 스크린샷으로 눈에 보이게 검증할 수 없어, 대신 (a) `npx tsc --noEmit`으로 DOM 타입이 이미 Badging API를 포함함을 확인, (b) 헤드리스 Chromium이 실제로 이 API를 지원한다는 걸 이용해 `page.evaluate`로 `setAppBadgeCount(3)`→`(0)`→`clearAppBadge()`를 실행시켜 페이지 에러 없이 세 호출 모두 통과함을 확인 — 실제 OS 아이콘에 숫자가 뜨는지 육안 확인은 사람이 실제 설치 환경에서 해야 한다고 문서에 명시. `npm run build`/`npm run lint`(기존 11건 유지) 통과 후 배포.
- **사이클 27 (2026-08-22):** "loading screen perceived wait time skeleton progress AI generation UX" 검색 → 진행률 표시나 참여 콘텐츠가 있으면 체감 대기 시간과 이탈률이 크게 줄어든다는 결과(진행 표시가 있으면 실제 9초를 22.6초로 견딜 만큼 체감이 늘어남). `GoalCreateScreen.tsx`의 AI 콘텐츠 생성(10~20초) 중엔 스피너와 고정 문구("💚 하루 5분이면 충분해요")만 있었고, 같은 패턴을 쓰는 `AssignmentCreateScreen.tsx`("문제 후보 만들기")도 마찬가지였음. 실제 생성 진행률을 알 방법이 없는 단일 API 호출이라 가짜 퍼센트 진행바 대신, 이 앱이 이미 쓰는 학습 메커니즘(인출 연습·라이트너 상자·인터리빙·스트릭 프리즈·자기설명 효과)을 짧게 설명하는 팁을 2.5초마다 순환 노출하는 쪽으로 F-73 채택 — 대기 시간에 의미를 더하면서 앱 사용법도 자연스럽게 알려줌. `src/data/loadingTips.ts`(팁 6개)와 `src/components/LoadingTips.tsx`(마운트된 동안만 인터벌로 순환, 무작위 시작)를 새로 만들고 `GoalCreateScreen.tsx`의 안내 박스에 `loading` 상태일 때만 꽂았다. `aria-live`는 의도적으로 안 붙였다 — 자주 바뀌는 부가 정보를 스크린 리더가 계속 읽으면 오히려 방해가 된다는 F-68의 판단을 그대로 따름. Node로 인덱스 순환(무작위 시작→+1→끝에서 랩어라운드) 로직만 시뮬레이션 확인(로그인 필요한 화면이라 실제 렌더링 확인은 불가, F-58/F-68과 동일한 제약) + `npm run build`/`npm run lint`(기존 11건 유지) 통과 후 배포. `AssignmentCreateScreen.tsx`에도 같은 패턴을 적용할지는 다음 사이클 후보로 남김(트래픽이 더 큰 목표 생성 화면 하나로 이번 사이클 스코프를 좁힘).
- **사이클 28 (2026-08-22):** F-73이 스스로 남긴 판단 보류 항목을 그대로 채택 — 새로 검색하지 않고 직전 사이클의 후속 작업을 처리. `AssignmentCreateScreen.tsx`의 "AI로 문제 후보 만들기"가 `GoalCreateScreen.tsx`와 정확히 같은 `generateGoalContent()` 호출·같은 10~20초 대기를 갖고 있는데도 "문제 만드는 중…" 버튼 라벨만 바뀔 뿐 팁이 없었던 걸 재확인. F-74로 F-73의 `LoadingTips` 컴포넌트를 새 컴포넌트나 새 팁 데이터 없이 그대로 import해 생성 버튼 아래 붙였다 — 완전히 같은 대기 상황이라 재사용이 정확히 맞는 사례. 새 순환 로직이 없으므로 F-73에서 이미 검증한 인덱스 로직을 다시 시뮬레이션할 필요 없이 타입체크·빌드로 연결 지점만 확인 + `npm run lint`(기존 11건 유지) 통과 후 배포. 이걸로 이 앱에서 Gemini AI 콘텐츠 생성을 기다리는 화면 두 곳 모두 학습 팁을 보여주게 됨.
- **사이클 29 (2026-08-22):** "self-determination theory autonomy choice learning app" 검색은 선택권을 늘리라는 일반론이라 이 앱의 "시작 마찰 제로" 원칙과 방향이 안 맞아 채택 보류. Duolingo UX 패턴 후보(콤보 배너, 마스코트 리액션)도 이미 F-36/`getMascotFace`로 구현돼 있음을 확인. 대신 `window.confirm(` 사용처를 grep해 이 앱이 네이티브 confirm 대신 항상 "취소/삭제 확인" 두 단계 인라인 UI를 쓴다는 패턴을 발견하고, 모든 삭제 흐름(`GoalListScreen`, `ClassDetailScreen`의 공지·숙제·자료)을 하나씩 대조하다가 `RecordingsScreen.tsx`(목소리 답변/오답 설명 녹음 목록)만 ✕ 한 번에 확인 없이 바로 영구 삭제된다는 걸 발견 — 되돌릴 수 없는 사용자 콘텐츠인데 이 앱의 다른 삭제 흐름과 일관성이 없었음. F-75로 `confirmingId` 상태를 추가해 GoalListScreen과 같은 "취소/삭제 확인" 두 단계 패턴으로 통일. 다른 화면은 아이템별 컴포넌트가 분리돼 로컬 상태로 확인을 관리하지만, RecordingsScreen은 한 컴포넌트에서 목록을 그리므로 "지금 확인 중인 id 하나"만 기억하는 방식을 택함. Playwright로 컴파일된 CSS를 이용해 (기본: ✕만 보임)/(확인 중: 취소·삭제 확인 버튼 쌍) 두 상태를 정적 HTML로 렌더링해 레이아웃 확인 + `npm run build`/`npm run lint`(기존 11건 유지) 통과 후 배포.
